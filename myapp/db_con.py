# from sqlalchemy import create_engine
# from django.conf import settings

# db_settings = settings.DATABASES['default']
# db_urls = (f"mssql+pyodbc://{db_settings['USER']}:{db_settings['PASSWORD']}@{db_settings['HOST']}:"
#            f"{db_settings['PORT']}/{db_settings['NAME']}?driver=ODBC+Driver+18+for+SQL+Server")
# engine = create_engine(db_urls)
